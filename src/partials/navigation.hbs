<nav>
    <ul>
        {{#foreach navigation}}
            <li class="{{slug}}{{#if current}} -active{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>
        {{/foreach}}
    </ul>

    <a href="{{@blog.url}}/rss/">Subscribe</a>
</nav>